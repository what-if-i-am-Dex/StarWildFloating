<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" 
        Topmost="True" MouseDown="Window_MouseDown">
    <StackPanel>
        <!-- 添加阴影和字体设置 -->
        <TextBlock Text="欢迎使用WPF!" FontSize="22" FontFamily="YouYuan">
            <TextBlock.Effect>
                <!-- DropShadowEffect 实现立体效果 -->
                <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="4" Opacity="0.5"/>
            </TextBlock.Effect>
            <TextBlock.Foreground>
                <!-- 设置文字颜色为白色 -->
                <SolidColorBrush Color="White"/>
            </TextBlock.Foreground>
        </TextBlock>
        <TextBlock Text="去设置中激活Windows." FontFamily="YouYuan" FontSize="22" FontWeight="Bold">
            <TextBlock.Effect>
                <!-- DropShadowEffect 实现立体效果 -->
                <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="4" Opacity="0.5"/>
            </TextBlock.Effect>
            <TextBlock.Foreground>
                <!-- 设置文字颜色为白色 -->
                <SolidColorBrush Color="White"/>
            </TextBlock.Foreground>
        </TextBlock>
    </StackPanel>
</Window>
